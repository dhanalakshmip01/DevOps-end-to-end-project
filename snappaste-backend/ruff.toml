target-version = "py311"
line-length = 100

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501", "B008"]  # B008: Depends() in defaults is standard FastAPI pattern

[lint.per-file-ignores]
"alembic/*" = ["I001"]  # alembic files have unconventional import ordering
